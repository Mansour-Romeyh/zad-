<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- All app traffic (API, product images). The Flutter template only
         declares this in the debug/profile manifests, so without it here
         RELEASE builds are denied every socket operation — DNS lookups and
         connections alike fail while debug runs work. -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <!-- PRD F4/A3 — "use my current location" in the address form
         (geolocator). Coarse is the graceful fallback when the user grants
         approximate location only. -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
    <!-- FCM push notifications (Notification Studio broadcasts, order
         updates). Android 13+ shows a runtime prompt, requested via
         FirebaseMessaging.requestPermission on first login. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <application
        android:label="Zad"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:allowBackup="false"
        android:fullBackupContent="false"><!-- Auto-backup restores flutter_secure_storage's encrypted prefs
             onto devices whose Keystore key was NOT restored (keys are
             device-bound and never backed up), so the ciphertext can't be
             decrypted → PlatformException/BadPaddingException on every read,
             which bricks login and every API call. Disabling backup removes
             that mismatch vector; auth tokens shouldn't sit in cloud backup
             anyway. -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
        <!-- Support popup "Call" button: url_launcher fires ACTION_VIEW on a
             tel: URI; Android 11+ package visibility needs this to resolve the
             dialer. (https/wa.me links rely on the implicit browser exemption.) -->
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="tel"/>
        </intent>
    </queries>
</manifest>
